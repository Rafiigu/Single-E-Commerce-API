meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: http://localhost:5000/api/payment-term
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:json {
  {
    "name": "Ovo"
  }
}

settings {
  encodeUrl: true
}
