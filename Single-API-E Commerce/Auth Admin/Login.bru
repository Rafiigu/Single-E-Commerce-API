meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:5000/api/auth/admin/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "superadmin@gmail.com",
    "password": "Superadmin123"
  }
}

script:post-response {
  if (res.status === 200) {
    const authToken = res.getBody().data.authToken
    bru.setVar("AUTH_TOKEN", authToken)
  }
}
