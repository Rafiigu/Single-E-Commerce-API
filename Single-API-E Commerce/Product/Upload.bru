meta {
  name: Upload
  type: http
  seq: 4
}

post {
  url: http://localhost:5000/api/product/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{AUTH_TOKEN}}
}

body:multipart-form {
  file: @file(D:\6.jpg)
}
